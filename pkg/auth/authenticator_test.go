package auth

import (
	"testing"
)

// TestNewAuthenticatedTadoClient tests creating an authenticated Tado client
// Note: Full integration tests would require real token storage and OAuth flow
func TestNewAuthenticatedTadoClient(t *testing.T) {
	t.Skip("Skipping integration test - requires real token storage and OAuth setup")
}
